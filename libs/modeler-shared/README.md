# `@egon/modeler-shared`

Private, browser-safe command classes shared by the VS Code host and EGN
webview. Message names and payloads are compatibility-sensitive. This package
must not acquire VS Code or renderer dependencies.
