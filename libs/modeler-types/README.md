# `@egon/modeler-types`

Browser-safe EGN document types and canonical default data. The package models
the EGN v4 document and retains the minimal legacy shape needed by icon
reconciliation. `createEmptyStory()` is the single default-story source for
production parsing and the webview preview.
