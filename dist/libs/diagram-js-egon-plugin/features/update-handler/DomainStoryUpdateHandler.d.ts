import { default as CommandStack } from 'diagram-js/lib/command/CommandStack';

export declare class DomainStoryUpdateHandler {
    static $inject: string[];
    constructor(commandStack: CommandStack);
}
